#ifndef MUTEX_GUARD_LOCAL_H
#define MUTEX_GUARD_LOCAL_H

// Redirect to the shared library version of MutexGuard
// This file exists for backward compatibility with code that includes "utils/MutexGuard.h"
#include <MutexGuard.h>

#endif // MUTEX_GUARD_LOCAL_H
